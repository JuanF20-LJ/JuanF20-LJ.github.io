---
const { selectedItem } = Astro.props;
---

<aside
  data-animation="default"
  data-collapse="medium"
  data-duration="400"
  data-easing="ease"
  data-easing2="ease"
  role="banner"
  class="sidebar w-nav"
>
  <div class="nav-content">
    <a href="/" class="w-nav-brand" aria-label="home">
      <div class="niv-data">
        <img
          src="/assets/juan.jpeg"
          loading="eager"
          alt="Profile picture"
          class="niv-image"
        />
        <div>
          <div class="label-white">Juan Felipe</div>
          <div class="label-gray">Ingeniero de Sistemas</div>
        </div>
      </div>
    </a>

    <nav role="navigation" class="w-nav-menu">
      <a
        href="/"
        class={`nav-link-container w-inline-block ${
          selectedItem === 'home' ? 'w--current' : ''
        }`}
      >
        <img
          src="/assets/home.svg"
          loading="eager"
          alt=""
          class="nav-link-image"
        />
        <div>Principal</div>
      </a>
      <a
        href="/presentacion"
        class={`nav-link-container w-inline-block ${
          selectedItem === 'presentacion' ? 'w--current' : ''
        }`}
        aria-current={
          selectedItem === 'presentacion' ? 'page' : undefined
        }
      >
        <img
          src="/assets/favorite.svg"
          loading="eager"
          alt=""
          class="nav-link-image"
        />
        <div>Presentación</div>
      </a>
      <div class="dropdown-container">
        <div
          class={`nav-link-container dropdown-toggle ${
            selectedItem?.startsWith('evidencias')
              ? 'w--current'
              : ''
          }`}
        >
          <img
            src="/assets/dns.svg"
            loading="eager"
            alt=""
            class="nav-link-image"
          />

          <div class="dropdown-title">
            <span>Evidencias</span>
            <span class="dropdown-arrow">▼</span>
          </div>
        </div>

        <div class="dropdown-menu">
          <a
            href="/modelado"
            class={`dropdown-item ${
              selectedItem === 'evidencias-modelado'
                ? 'w--current'
                : ''
            }`}
          >
            Modelado
          </a>

          <a
            href="/normalizacion"
            class={`dropdown-item ${
              selectedItem === 'evidencias-normalizacion'
                ? 'w--current'
                : ''
            }`}
          >
            Normalización
          </a>

          <a
            href="/sql"
            class={`dropdown-item ${
              selectedItem === 'evidencias-sql'
                ? 'w--current'
                : ''
            }`}
          >
            SQL
          </a>

          <a
            href="/proyecto"
            class={`dropdown-item ${
              selectedItem === 'evidencias-proyecto'
                ? 'w--current'
                : ''
            }`}
          >
            Proyecto
          </a>
        </div>
      </div>

      <a
        href="/evidencias"
        class={`nav-link-container w-inline-block ${
          selectedItem === 'evidencias-antiguas'
            ? 'w--current'
            : ''
        }`}
      >
        <img
          src="/assets/dns.svg"
          loading="eager"
          alt=""
          class="nav-link-image"
        />

        <div>Evidencias Antiguas</div>
      </a>
    </nav>

    <div
      class="navbar-icon-button w-nav-button"
      style="-webkit-user-select: text;"
      aria-label="menu"
      role="button"
      tabindex="0"
    >
      <img
        src="/assets/menu.svg"
        loading="eager"
        alt=""
        class="navbar-icon"
      />
    </div>
  </div>

  <script>
    import { setupNav } from '../scripts/nav.js';
    
    let cleanup;
    document.addEventListener('astro:page-load', () => {
      if (cleanup) cleanup();
      cleanup = setupNav();
    });

    document.addEventListener('astro:before-swap', () => {
      if (cleanup) cleanup();
    });
  </script>
</aside>
